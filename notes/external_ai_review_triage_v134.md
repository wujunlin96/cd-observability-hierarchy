# External AI review triage v134

Date: 2026-07-01

Scope: triage the fourth external review batch before public arXiv packaging.
The goal is to fix issues that affect the current preprint and to record
journal-level work without polluting the manuscript with project-management
language.

## Addressed in the manuscript/package

1. Abstract and introduction sharpen the value-add as a local no-go hierarchy
   plus the calibration-independent morphology vector \({\cal M}_{CD}\), not as
   novelty for multiple speeds by itself.
2. Notation now centralizes \(C\), \(D\), \(D/C\), phase conventions,
   \(\alpha_{\rm int}\), \(G_{\rm common}\), relative calibration connection,
   escape class, and \({\cal M}_{CD}\).
3. The preferred-field example keeps \(C\) explicit and derives
   \(D=C\sqrt{1+\xi}\) from the displayed metric instead of mixing coordinate
   and speed conventions.
4. Proposition A.1 now states the Schur-lemma/intertwiner argument directly.
5. Proposition A.2 is explicitly limited to ordinary spacetime point maps and
   uses little-group fixed-subspace reasoning.
6. Proposition A.4 now states that the common-cone result is local to one
   tangent space and must be redone with tetrads and relative transport in
   curved spacetime.
7. The interface section now separates the sufficient global-time condition
   from the scalar-interface calculation, so chronology protection is a model
   assumption rather than a claimed theorem.
8. The finite-wall figure now includes a small cone-splitting comparison
   \(D/C=\sqrt{1+10^{-3}}\), and the caption explains the horizontal axis.
9. The one-dimensional scalar interface model now states what survives and
   what changes in four-dimensional photon or fermion portals.
10. The escape-class table is organized by the primary no-go assumption being
    broken, reducing overlap between interface, multimetric, and portal routes.
11. Discussion is separated from limitations and compares the present framework
    with VSL, SME, disformal/aether, photon portals, neutrino shortcuts,
    Weyl/nonmetricity, clock-network searches, and multimetric gravity.
12. A one-command reproduction script was added for public repository users.

## Recorded for journal upgrade

1. Replace Appendix D's tree-level SME sketch with a portal-specific operator
   dictionary, including field redefinitions, minimal/nonminimal SME placement,
   and explicit coefficient bounds.
2. Compute loop-induced visible Lorentz violation for the chosen portal and
   state the technical-naturalness mechanism or fine tuning.
3. Build a quantitative data-facing forecast or exclusion table for one channel
   rather than relying on scale estimates.
4. Promote the global-time sufficient condition into a theorem for a specified
   wall-network geometry, or restrict the geometry enough that controllable
   closed causal curves are impossible by construction.
5. Derive the interface thickness \(L_w\) from wall microphysics.
6. Extend the finite-wall model to a physically interpretable four-dimensional
   channel with spin or polarization and multichannel unitarity.
7. Refresh the literature boundary at submission time for the selected
   journal route; do not add unsourced "latest" claims without live checking.
